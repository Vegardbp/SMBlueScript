If you can miraculously compile an exe file from this, place it into the blueprint file for a creation you want to
overwrite, write your code into a code.txt in the same file, and run the exe. To learn how to use it, look at the
example libraries... good luck. Warning, the syntax is hideous.

You can use debug, debugBlue, or print to debug the code